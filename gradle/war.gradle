apply plugin: "war"

bootWar {
    mainClassName = "com.fleta.watchingservice.WatchingServiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
